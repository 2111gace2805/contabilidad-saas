import { ModuleNotAvailable } from '../common/ModuleNotAvailable';

export function AccountsPayable() {
  return <ModuleNotAvailable moduleName="Cuentas por Pagar" description="Gestiona las cuentas por pagar desde el módulo de Compras." />;
}
